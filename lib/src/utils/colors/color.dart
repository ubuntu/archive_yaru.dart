import 'dart:ui';
import '../../../yaru.dart';

// Ubuntu color palette `https://design.ubuntu.com/brand/colour-palette/`

class YaruColor {
  const YaruColor._();
  static const bakersChocolate = Color(YaruHex.bakersChocolate);
  static const black = Color(YaruHex.black);
  static const bouquet = Color(YaruHex.bouquet);
  static const bouquet2 = Color(YaruHex.bouquet2);
  static const bonJour = Color(YaruHex.bonJour);
  static const bonJour2 = Color(YaruHex.bonJour2);
  static const burntSienna = Color(YaruHex.burntSienna);
  static const burntUmber = Color(YaruHex.burntUmber);
  static const canonicalAubergine = Color(YaruHex.canonicalAubergine);
  static const cannonPink = Color(YaruHex.cannonPink);
  static const cannonPink2 = Color(YaruHex.cannonPink2);
  static const cloud = Color(YaruHex.cloud);
  static const coolGrey = Color(YaruHex.coolGrey);
  static const darkAubergine = Color(YaruHex.darkAubergine);
  static const darkSalmon = Color(YaruHex.darkSalmon);
  static const desertStorm = Color(YaruHex.desertStorm);
  static const desertStorm2 = Color(YaruHex.desertStorm2);
  static const disabledGreyDark = Color(YaruHex.disabledGreyDark);
  static const eggplant = Color(YaruHex.eggplant);
  static const errorColor = Color(YaruHex.errorColor);
  static const falcon = Color(YaruHex.falcon);
  static const green = Color(YaruHex.green);
  static const lightAubergine = Color(YaruHex.lightAubergine);
  static const lilac = Color(YaruHex.lilac);
  static const lily = Color(YaruHex.lily);
  static const lily2 = Color(YaruHex.lily2);
  static const lola = Color(YaruHex.lola);
  static const lola2 = Color(YaruHex.lola2);
  static const lola3 = Color(YaruHex.lola3);
  static const manhattan = Color(YaruHex.manhattan);
  static const midAubergine = Color(YaruHex.midAubergine);
  static const mountbattenPink = Color(YaruHex.mountbattenPink);
  static const oldLavender = Color(YaruHex.oldLavender);
  static const paleSlate = Color(YaruHex.paleSlate);
  static const pippin = Color(YaruHex.pippin);
  static const prim = Color(YaruHex.prim);
  static const red = Color(YaruHex.red);
  static const roseWhite = Color(YaruHex.roseWhite);
  static const rust = Color(YaruHex.rust);
  static const sealBrown = Color(YaruHex.sealBrown);
  static const shadyLady = Color(YaruHex.shadyLady);
  static const softPeach = Color(YaruHex.softPeach);
  static const textGrey = Color(YaruHex.textGrey);
  static const tide = Color(YaruHex.tide);
  static const ubuntuOrange = Color(YaruHex.ubuntuOrange);
  static const warmGrey = Color(YaruHex.warmGrey);
  static const westar = Color(YaruHex.westar);
  static const white = Color(YaruHex.white);
}
