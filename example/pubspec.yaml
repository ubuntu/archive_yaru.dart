name: yaru_example
description: Demonstrates Yaru theming
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  handy_window: ^0.1.2
  yaru:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  path: ^1.8.3

flutter:
  uses-material-design: true
